# Optional: API origin only (no path). The app posts to ${VITE_API_BASE_URL}/api/submit.
# Example: VITE_API_BASE_URL=https://api.yourdomain.com
#
# Leave unset to use same-origin /api/submit (Cloudflare Pages Functions in ../functions/ or reverse proxy).
# VITE_API_BASE_URL=
